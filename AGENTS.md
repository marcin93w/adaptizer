# AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues (marcin93w/adaptizer), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context — root `CONTEXT-MAP.md` plus a `CONTEXT.md` and `docs/adr/` per component (`API`, `Instrument`, `InstrumentUI`, `Player`). See `docs/agents/domain.md`.
