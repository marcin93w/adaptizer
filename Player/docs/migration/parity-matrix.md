# Parity matrix

Tracks every observable legacy behavior documented in `docs/migration/M00-baseline.md` against its React Native replacement. This file is seeded at M00/M01 time: every `RN evidence` cell is `pending` and every `Status` cell is `⬜ not verified`, per the M00 review boundary (documentation and evidence only, no proposed behavior changes). `Legacy evidence` cells reference the implementing source (`file:line`) as a static code reference; where a behavior can only be confirmed by observing the running app, that is called out explicitly rather than claimed as verified - see `docs/migration/M00-baseline.md` section 7 ("Manual evidence required", NOT YET CAPTURED).

Update this table as later migration steps (A0x/B0x/C0x/D0x/I01/I02) produce real evidence. Do not mark a row `✅` without attaching evidence. Use the `Approved difference?` column only when a reviewer has explicitly signed off on an intentional behavior change (Gate 5 in the migration plan); leave it `—` otherwise.

| ID | Behavior | Legacy evidence | RN evidence | Status | Approved difference? |
| --- | --- | --- | --- | --- | --- |
| PAR-01 | App launch | Code reference (static, not runtime-verified): `MainActivity.kt:31-75` (`onCreate` - view binding, input registration, player construction, catalog fetch kickoff) | pending | ⬜ not verified | — |
| PAR-02 | Catalog fetch success | Code reference: `SongsRepository.kt:33-40` (`fetchSongs()` calling `api.getSongs()` against the songs endpoint), consumed at `MainActivity.kt:64-65` | pending | ⬜ not verified | — |
| PAR-03 | Catalog empty | Code reference: `SongsRepository.kt:33-40` returns `emptyList()` both for a legitimately empty catalog and for any exception (see M00 Known defect 5 - the two are indistinguishable); `MainActivity.kt:73` would throw on `songs[0]` if reached with an empty list (M00 Known defect 1) | pending | ⬜ not verified | — |
| PAR-04 | Catalog network failure | Code reference: `SongsRepository.kt:36-39` catches `Exception`, calls `e.printStackTrace()`, returns `emptyList()` - no distinct failure state exists in the current code (M00 Known defect 5) | pending | ⬜ not verified | — |
| PAR-05 | Song list rendering | Code reference: `SongsAdapter.kt:22-35` (`onCreateViewHolder`/`onBindViewHolder`/`bind`), `song_list_item.xml` (`songTitle`, `songArtist` bindings) | pending | ⬜ not verified | — |
| PAR-06 | Song selection | Code reference: `SongsAdapter.kt:28-33` (item click invokes listener), `MainActivity.kt:68-70` (`adapter.setOnItemClickListener { playSong(it) }`) | pending | ⬜ not verified | — |
| PAR-07 | First-song auto-prepare | Code reference: `MainActivity.kt:73` (`prepareSong(songs[0])`, unconditional - M00 Known defect 1) | pending | ⬜ not verified | — |
| PAR-08 | Play | Code reference: `MainActivity.kt:77-80` (`playSong` calls `prepareSong` then `exoPlayer.play()`); also reachable via the `PlayerView` built-in controller (`MainActivity.kt:59-61`). Runtime confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-09 | Pause | No explicit `exoPlayer.pause()` call exists in the Kotlin source; only reachable via the `PlayerView` built-in controller (`MainActivity.kt:59-61`). Runtime confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-10 | Seek | No explicit `exoPlayer.seekTo(...)` call exists in the Kotlin source; only reachable via the `PlayerView` built-in controller seek bar (`MainActivity.kt:59-61`). Runtime confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-11 | Now-playing title format `"{author} - {name}"` | Code reference: `MainActivity.kt:84` - `nowPlayingTitle.text = "${song.author} - ${song.name}"` | pending | ⬜ not verified | — |
| PAR-12 | Intensity bar 0-9 | Code reference: `activity_main.xml:50-57` (`ProgressBar android:max="10"`), `MainActivity.kt:51` (`intensityProgressBar.progress = adaptizer.getTrackIndex()`), `AdaptizerState.kt:8-11` (formula bounded to 0-9 for in-range inputs) | pending | ⬜ not verified | — |
| PAR-13 | Volume -> intensity response | Code reference: `VolumeInput.kt:11-16` (normalization), `VolumeInput.kt:18-28` (change listener via `VOLUME_CHANGED_ACTION`), `Adaptizer.kt:15-18` (state recompute). Runtime confirmation NOT YET CAPTURED - requires a device with an adjustable stream volume; see M00 section 7 | pending | ⬜ not verified | — |
| PAR-14 | Shake -> intensity response | Code reference: `AccelerometerInput.kt:47-81` (sensor magnitude, clamp to 0-9, 2s throttle interval, 2s stop-delay). Runtime confirmation NOT YET CAPTURED - requires a physical device with a working accelerometer; see M00 section 7 | pending | ⬜ not verified | — |
| PAR-15 | Track switch at index 0 | Code reference: `AdaptizerTrackSelector.kt:17-46` (`selectTracks`/`changeTrack`), `AdaptizerTrackSelection.kt:40-43` (`setSelectedTrack`). Audible/representation-level confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-16 | Track switch at index 4 | Same code reference as PAR-15. Audible/representation-level confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-17 | Track switch at index 9 | Same code reference as PAR-15. Audible/representation-level confirmation NOT YET CAPTURED - see M00 section 7 | pending | ⬜ not verified | — |
| PAR-18 | Queue invalidation on switch | Code reference: `AdaptizerTrackSelection.kt:32-43` (`clearQueue` flag set by `setSelectedTrack`, consumed once by `evaluateQueueSize`, which returns `0` to drop queued chunks) | pending | ⬜ not verified | — |
| PAR-19 | Player error surfaced | No implementation found: `MainActivity.kt` never calls `exoPlayer.addListener(...)`; no `Player.Listener`/`onPlayerError` handling exists anywhere in the source tree today (M00 Known defect 7, "additional defects" list) | pending | ⬜ not verified | — |
| PAR-20 | Teardown releases player + inputs | Code reference: `MainActivity.kt:95-99` (`onDestroy()` calls `exoPlayer.release()` and `inputs.forEach { it.release() }`); note `VolumeInput.release()` and `AccelerometerInput`'s scope cancellation are incomplete (M00 Known defects 2 and 3) | pending | ⬜ not verified | — |

**Legend.** Status: `⬜ not verified` (seeded default) / `🟨 in progress` / `✅ verified` / `❌ regression found`. Approved difference: `—` (none recorded) or a short note plus the reviewer/PR that approved it.
